# 时间序列预测Web应用 - 项目概览

## 📊 项目简介

这是一个基于ARIMA模型的时间序列预测Web应用，专门用于股票价格预测和回测检验。该应用提供了完整的用户界面，支持Excel文件上传、一步向前预测、滚动回测检验和结果导出功能。

## 🎯 主要功能

### 1. 数据上传与验证
- ✅ 支持.xlsx格式Excel文件上传
- ✅ 自动验证数据格式（必填列'y'，可选列'date'）
- ✅ 检测缺失值并提示错误
- ✅ 显示数据统计信息（样本数、均值、标准差等）

### 2. 一步向前预测
- ✅ 使用全部历史数据预测下一个时间点
- ✅ 基于ARIMA(2,1,2)模型
- ✅ 实时显示预测结果

### 3. 回测检验（滚动预测）
- ✅ 80%训练集 + 20%测试集划分
- ✅ 严格的时间顺序滚动预测
- ✅ **无数据泄露**：每个预测仅使用历史数据
- ✅ 提供多种评估指标（MSE、MAE、RMSE）

### 4. 结果导出
- ✅ 导出测试集预测结果到Excel
- ✅ 保留原始日期列（如果存在）
- ✅ 标准化的输出格式

## 🛠️ 技术栈

### 后端
- **框架**: Flask 3.0.0
- **数据处理**: pandas 2.1.4, numpy 1.26.2
- **时间序列模型**: statsmodels 0.14.0 (ARIMA)
- **评估指标**: scikit-learn 1.3.2
- **Excel处理**: openpyxl 3.1.2

### 前端
- **技术**: 原生HTML5 + CSS3 + JavaScript
- **设计**: 响应式布局，渐变配色方案
- **交互**: 拖拽上传，实时反馈

## 📁 项目结构

```
finalexam/
├── app.py                  # Flask后端应用主文件
├── requirements.txt        # Python依赖包列表
├── templates/
│   └── index.html         # 前端用户界面
├── dataset.xlsx           # 示例数据集（500个样本）
├── README.md              # 项目说明文档
├── DEPLOYMENT.md          # 部署指南
├── .gitignore            # Git忽略文件配置
├── Procfile              # Render部署配置
├── runtime.txt           # Python版本配置
├── start.bat             # Windows快速启动脚本
├── start.sh              # Linux/Mac快速启动脚本
├── test_api.py           # API功能测试脚本
└── analyze_data.py       # 数据分析脚本
```

## 🚀 快速开始

### 方法一：使用启动脚本（推荐）

**Windows用户：**
```bash
双击运行 start.bat
```

**Linux/Mac用户：**
```bash
chmod +x start.sh
./start.sh
```

### 方法二：手动启动

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 启动应用
python app.py

# 3. 访问应用
# 在浏览器中打开: http://localhost:5000
```

## 📈 预测方法详解

### ARIMA模型参数

本应用使用ARIMA(p,d,q)模型进行时间序列预测：

- **p = 2**: 自回归阶数，使用过去2个时间点的值
- **d = 1**: 差分阶数，使时间序列平稳
- **q = 2**: 滑动平均阶数，使用过去2个预测误差

### 模型选择策略

```python
def _fit_arima(self, data):
    try:
        # 优先使用ARIMA(2,1,2)
        model = ARIMA(data, order=(2, 1, 2))
        fitted_model = model.fit()
        return fitted_model
    except:
        # 失败时降级到ARIMA(1,1,1)
        model = ARIMA(data, order=(1, 1, 1))
        fitted_model = model.fit()
        return fitted_model
```

### 预测原理

#### 一步向前预测
- 使用全部历史数据：y₁, y₂, ..., yₙ
- 预测目标：yₙ₊₁
- **无数据泄露**：仅使用历史信息

#### 滚动预测（回测）
- 训练集：前80%数据
- 测试集：后20%数据
- 对每个测试点t：
  - 使用t之前的所有数据拟合模型
  - 预测t点的值
  - **绝不使用t点或未来数据**

## 🧪 测试结果

使用提供的dataset.xlsx（500个样本）测试：

### 数据统计
- 样本数量：500
- 均值：1.358985
- 标准差：0.008434
- 最小值：1.325965
- 最大值：1.382465

### 预测性能
- 一步向前预测值：1.354252
- 回测MSE：0.000018
- 回测MAE：0.003437
- 回测RMSE：0.004266

### 导出结果
- 测试集大小：100个样本
- 输出格式：Excel文件
- 包含列：y（预测值）, date（日期）

## 🔒 数据安全与隐私

- ✅ **无数据泄露**：严格的时间顺序预测
- ✅ **本地处理**：数据在服务器端处理，不存储
- ✅ **临时文件**：上传的文件在使用后自动删除
- ✅ **大小限制**：文件大小限制为16MB

## 📊 API接口文档

### 1. 上传文件
```
POST /api/upload
Content-Type: multipart/form-data
Parameters: file (Excel文件)
Response: {
  "success": true,
  "message": "成功加载 N 个样本",
  "summary": {...}
}
```

### 2. 一步向前预测
```
POST /api/forecast
Response: {
  "success": true,
  "forecast": 1.354252,
  "message": "下一个时间点的预测值: 1.354252"
}
```

### 3. 回测检验
```
POST /api/backtest
Response: {
  "success": true,
  "result": {
    "predictions": [...],
    "test_data": [...],
    "metrics": {
      "mse": 0.000018,
      "mae": 0.003437,
      "rmse": 0.004266
    },
    "train_size": 400,
    "test_size": 100
  }
}
```

### 4. 导出结果
```
POST /api/export
Response: Excel文件下载
```

## 🌐 部署选项

### 推荐平台

1. **Render.com** (推荐)
   - 免费套餐
   - 自动部署
   - 全球CDN

2. **Railway.app**
   - 免费套餐
   - 简单易用
   - GitHub集成

3. **PythonAnywhere**
   - 教育友好
   - 稳定可靠
   - 免费教育版

详细部署指南请参考 [DEPLOYMENT.md](DEPLOYMENT.md)

## 📝 数据格式要求

### 输入文件
- **格式**: .xlsx
- **必填列**: `y`（时间序列数据）
- **可选列**: `date`（日期信息）
- **要求**: 
  - y列无缺失值
  - 按时间从早到晚排序

### 输出文件
- **格式**: .xlsx
- **包含列**: `y`（预测值）
- **可选列**: `date`（如果输入有日期列）

## 🎨 用户界面特点

- **现代化设计**: 渐变背景，卡片式布局
- **响应式**: 适配各种屏幕尺寸
- **交互友好**: 拖拽上传，实时反馈
- **错误处理**: 清晰的错误提示
- **加载状态**: 动画加载指示器

## 🏆 项目亮点

1. **严格的时间序列预测**: 完全避免数据泄露
2. **完整的评估体系**: 多种性能指标
3. **用户友好**: 直观的界面和操作流程
4. **生产就绪**: 错误处理，日志记录
5. **易于部署**: 支持多种云平台
6. **文档完善**: 详细的使用和部署指南

## 📚 技术要点

### 时间序列预测
- ARIMA模型的理论基础
- 参数选择和优化
- 模型诊断和验证

### 软件工程
- RESTful API设计
- 前后端分离架构
- 错误处理和日志记录
- 代码组织和模块化

### 用户体验
- 响应式设计
- 交互反馈
- 错误提示
- 加载状态

## 🔧 维护和扩展

### 可能的改进
1. 支持更多时间序列模型（Prophet, LSTM等）
2. 添加可视化图表
3. 支持批量文件处理
4. 添加用户认证
5. 数据库存储历史预测

### 性能优化
1. 缓存模型拟合结果
2. 异步处理长时间任务
3. 数据库查询优化
4. 前端资源压缩

## 📞 技术支持

如有问题，请参考：
- [README.md](README.md) - 基本使用说明
- [DEPLOYMENT.md](DEPLOYMENT.md) - 部署指南
- 代码注释 - 详细的实现说明

## 📄 许可证

本项目仅供学习和教育使用。

---

**开发完成时间**: 2026-04-29
**版本**: 1.0.0
**状态**: 生产就绪 ✅